# Django Settings
DEBUG=True

# CORS Settings - Set to True for development, False for production
CORS_ALLOW_ALL_ORIGINS=True

# Allowed Hosts (comma-separated)
ALLOWED_HOSTS=localhost,127.0.0.1,catabatic-sulema-unsubmerged.ngrok-free.dev

# CORS Allowed Origins (comma-separated) - Only needed if CORS_ALLOW_ALL_ORIGINS=False
CORS_ALLOWED_ORIGINS=https://catabatic-sulema-unsubmerged.ngrok-free.dev,http://localhost:8080,http://localhost:3000

# CSRF Trusted Origins (comma-separated)
CSRF_TRUSTED_ORIGINS=https://catabatic-sulema-unsubmerged.ngrok-free.dev,http://localhost:8080

# Swagger Base URL - This is the URL that Swagger will use to make API requests
# IMPORTANT: This should be your ngrok URL or the public URL where your API is accessible
SWAGGER_BASE_URL=https://catabatic-sulema-unsubmerged.ngrok-free.dev

# Email Configuration
# For Gmail, you need to use an App Password (not your regular password)
# Go to: https://myaccount.google.com/apppasswords
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password
DEFAULT_FROM_EMAIL=your-email@gmail.com

# For development/testing, you can use console backend to print emails to terminal
# EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
